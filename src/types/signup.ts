export type SignupPayload = {
  name: string;
  email: string;
  role: string;
  companySize: string;
  useCase: string;
};

export type WelcomeCopy = {
  headline: string;
  body: string;
  ctaText: string;
};
